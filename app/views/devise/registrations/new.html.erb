<h2>Sign up</h2>
<div class="row">
  <div class="col s8 push-s2">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>
      <%= render partial: "layouts/user_sign_up_form", locals: { f: f } %>
    <% end %>
    <%= render "devise/shared/links" %>
</div>
</div>
